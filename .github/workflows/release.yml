---
on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    uses: "Mosher-Labs/.github/.github/workflows/release.yml@v0.3.0"

  heimdallr:
    uses: "./.github/workflows/slack.yml"
    with:
      message: "Testing release slack messages."
