---
on:
  pull_request:
  push:
    branches:
      - main

permissions: read-all

jobs:
  pre-commit:
    uses: "Mosher-Labs/.github/.github/workflows/pre-commit.yml@main"

  heimdallr:
    needs: pre-commit
    uses: "Mosher-Labs/.github/.github/workflows/heimdallr.yml@heimdallr"
    secrets: inherit
    with:
      message: "Testing pre-commit slack messages."
